apiVersion: v1
kind: Pod
metadata:
  name: myapp
  labels:
    name: myapp
spec:
  containers:
  - name: myapp
    image: nginx
    resources:
      requests:
        memory: 10Mi
        cpu: 1m
      limits:
        memory: 50Mi
        cpu: 20m
    ports:
      - containerPort: 80

